-- Выполнить в SQL Editor Supabase
-- Удаляет обратимо зашифрованные копии паролей: пароли теперь живут
-- только в Supabase Auth (хеш). Код больше не пишет и не читает password_enc.
-- ВАЖНО: перед выполнением убедитесь, что задеплоена версия кода без
-- password_enc (иначе просмотр паролей в /profiles перестанет работать —
-- это и есть цель: пароли нельзя просматривать, только задавать заново).
-- Безопасно перезапускать (идемпотентно).

ALTER TABLE profiles DROP COLUMN IF EXISTS password_enc;
