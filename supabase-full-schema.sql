-- Полная схема, СГЕНЕРИРОВАНА из живой базы (все таблицы: колонки, типы, default, PK).
-- Выполнить ПЕРВЫМ в новом проекте, затем fix-rls, clients-rpc.
-- test_clients намеренно исключён (тестовая копия).

CREATE EXTENSION IF NOT EXISTS pgcrypto;

CREATE TABLE IF NOT EXISTS client_activity (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  client_table TEXT DEFAULT '',
  client_id BIGINT DEFAULT 0,
  client_name TEXT DEFAULT '',
  action TEXT DEFAULT '',
  message TEXT DEFAULT '',
  actor_id TEXT,
  actor_name TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  changes JSONB
);

CREATE TABLE IF NOT EXISTS clients_arenda (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  date TEXT DEFAULT '',
  name TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  district TEXT DEFAULT '',
  amount INTEGER DEFAULT 0,
  furniture TEXT DEFAULT '',
  rental_period TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  who_lives TEXT DEFAULT '',
  people_count INTEGER DEFAULT 1,
  notes TEXT DEFAULT '',
  completed TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  type TEXT DEFAULT '',
  area TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  contract TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  plot_type TEXT DEFAULT '',
  purpose TEXT DEFAULT '',
  communications TEXT DEFAULT '',
  access TEXT DEFAULT '',
  plot_shape TEXT DEFAULT '',
  relief TEXT DEFAULT '',
  restrictions TEXT DEFAULT '',
  documents TEXT DEFAULT '{}',
  reason TEXT DEFAULT '',
  status_comment TEXT DEFAULT '',
  resume_date TEXT DEFAULT '',
  preferences TEXT DEFAULT '',
  client_category TEXT DEFAULT '',
  tags TEXT DEFAULT '[]',
  premise_type TEXT DEFAULT '',
  finishing TEXT DEFAULT '',
  contract_type TEXT DEFAULT '',
  contract_kind TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS clients_pokupka (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  date TEXT DEFAULT '',
  name TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  district TEXT DEFAULT '',
  amount INTEGER DEFAULT 0,
  furniture TEXT DEFAULT '',
  rental_period TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  who_lives TEXT DEFAULT '',
  people_count INTEGER DEFAULT 1,
  notes TEXT DEFAULT '',
  completed TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  type TEXT DEFAULT '',
  area TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  contract TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  plot_type TEXT DEFAULT '',
  purpose TEXT DEFAULT '',
  communications TEXT DEFAULT '',
  access TEXT DEFAULT '',
  plot_shape TEXT DEFAULT '',
  relief TEXT DEFAULT '',
  restrictions TEXT DEFAULT '',
  documents TEXT DEFAULT '{}'
);

CREATE TABLE IF NOT EXISTS clients_prodaja (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  date TEXT DEFAULT '',
  name TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  district TEXT DEFAULT '',
  amount INTEGER DEFAULT 0,
  furniture TEXT DEFAULT '',
  rental_period TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  who_lives TEXT DEFAULT '',
  people_count INTEGER DEFAULT 1,
  notes TEXT DEFAULT '',
  completed TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  type TEXT DEFAULT '',
  area TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  contract TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  plot_type TEXT DEFAULT '',
  purpose TEXT DEFAULT '',
  communications TEXT DEFAULT '',
  access TEXT DEFAULT '',
  plot_shape TEXT DEFAULT '',
  relief TEXT DEFAULT '',
  restrictions TEXT DEFAULT '',
  documents TEXT DEFAULT '{}',
  reason TEXT DEFAULT '',
  status_comment TEXT DEFAULT '',
  resume_date TEXT DEFAULT '',
  preferences TEXT DEFAULT '',
  client_category TEXT DEFAULT '',
  tags TEXT DEFAULT '[]',
  premise_type TEXT DEFAULT '',
  finishing TEXT DEFAULT '',
  contract_type TEXT DEFAULT '',
  contract_kind TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS deals_kvartiry (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  client TEXT DEFAULT '',
  amount NUMERIC DEFAULT 0,
  stage TEXT DEFAULT 'Первичный контакт',
  date TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  category TEXT DEFAULT 'arenda',
  type TEXT DEFAULT 'Квартира',
  area TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  contract TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  district TEXT DEFAULT '',
  furniture TEXT DEFAULT '',
  rental_period TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  who_lives TEXT DEFAULT '',
  people_count INTEGER DEFAULT 1,
  notes TEXT DEFAULT '',
  completed TEXT DEFAULT 'В процессе',
  broker TEXT DEFAULT '',
  layout TEXT DEFAULT '',
  renter_type TEXT DEFAULT '',
  payment TEXT DEFAULT '',
  finishing TEXT DEFAULT '',
  premise_type TEXT DEFAULT 'Отдельно стоящее здание',
  plot_type TEXT DEFAULT '',
  purpose TEXT DEFAULT '',
  communications TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  access TEXT DEFAULT '',
  plot_shape TEXT DEFAULT '',
  relief TEXT DEFAULT '',
  documents TEXT DEFAULT '',
  restrictions TEXT DEFAULT '',
  time TEXT DEFAULT '',
  number TEXT DEFAULT '',
  completion_date TEXT,
  commission NUMERIC DEFAULT 0,
  owner_name TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS deals_pomescheniya (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  client TEXT DEFAULT '',
  amount NUMERIC DEFAULT 0,
  stage TEXT DEFAULT 'Первичный контакт',
  date TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  category TEXT DEFAULT 'arenda',
  type TEXT DEFAULT '',
  area TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  contract TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  district TEXT DEFAULT '',
  furniture TEXT DEFAULT '',
  rental_period TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  who_lives TEXT DEFAULT '',
  people_count INTEGER DEFAULT 1,
  notes TEXT DEFAULT '',
  completed TEXT DEFAULT 'В процессе',
  broker TEXT DEFAULT '',
  layout TEXT DEFAULT '',
  renter_type TEXT DEFAULT '',
  payment TEXT DEFAULT '',
  finishing TEXT DEFAULT '',
  premise_type TEXT DEFAULT 'Отдельно стоящее здание',
  plot_type TEXT DEFAULT '',
  purpose TEXT DEFAULT '',
  communications TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  access TEXT DEFAULT '',
  plot_shape TEXT DEFAULT '',
  relief TEXT DEFAULT '',
  documents TEXT DEFAULT '',
  restrictions TEXT DEFAULT '',
  time TEXT DEFAULT '',
  number TEXT DEFAULT '',
  completion_date TEXT,
  commission NUMERIC DEFAULT 0,
  owner_name TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS deals_zemlya (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  client TEXT DEFAULT '',
  amount NUMERIC DEFAULT 0,
  stage TEXT DEFAULT 'Первичный контакт',
  date TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  category TEXT DEFAULT 'arenda',
  type TEXT DEFAULT '',
  area TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  contract TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  district TEXT DEFAULT '',
  furniture TEXT DEFAULT '',
  rental_period TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  who_lives TEXT DEFAULT '',
  people_count INTEGER DEFAULT 1,
  notes TEXT DEFAULT '',
  completed TEXT DEFAULT 'В процессе',
  broker TEXT DEFAULT '',
  layout TEXT DEFAULT '',
  renter_type TEXT DEFAULT '',
  payment TEXT DEFAULT '',
  finishing TEXT DEFAULT '',
  premise_type TEXT DEFAULT 'Отдельно стоящее здание',
  plot_type TEXT DEFAULT '',
  purpose TEXT DEFAULT '',
  communications TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  access TEXT DEFAULT '',
  plot_shape TEXT DEFAULT '',
  relief TEXT DEFAULT '',
  documents TEXT DEFAULT '',
  restrictions TEXT DEFAULT '',
  time TEXT DEFAULT '',
  number TEXT DEFAULT '',
  completion_date TEXT,
  commission NUMERIC DEFAULT 0,
  owner_name TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS districts (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE IF NOT EXISTS lists (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  type TEXT,
  value TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE IF NOT EXISTS notifications (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  profile_id BIGINT,
  message TEXT DEFAULT '',
  type TEXT DEFAULT 'info',
  related_to TEXT DEFAULT '',
  is_read BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT now(),
  related_id BIGINT
);

CREATE TABLE IF NOT EXISTS objects (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  krisha_id BIGINT,
  deal_type TEXT DEFAULT '',
  prop_type TEXT DEFAULT '',
  title TEXT DEFAULT '',
  price BIGINT DEFAULT 0,
  price_text TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  area TEXT DEFAULT '',
  floor TEXT DEFAULT '',
  address TEXT DEFAULT '',
  description TEXT DEFAULT '',
  image_url TEXT DEFAULT '',
  krisha_url TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  is_active BOOLEAN DEFAULT TRUE,
  last_seen_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE IF NOT EXISTS owners_doma (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  district TEXT DEFAULT '',
  address TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  house_area TEXT DEFAULT '',
  land_area TEXT DEFAULT '',
  price NUMERIC DEFAULT 0,
  contract_type TEXT DEFAULT '',
  contract_kind TEXT DEFAULT '',
  status TEXT DEFAULT 'Новый собственник',
  condition TEXT DEFAULT '',
  notes TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  documents TEXT DEFAULT '[]',
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE IF NOT EXISTS owners_kvartiry (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  district TEXT DEFAULT '',
  address TEXT DEFAULT '',
  jk TEXT DEFAULT '',
  rooms TEXT DEFAULT '',
  area TEXT DEFAULT '',
  price NUMERIC DEFAULT 0,
  contract_type TEXT DEFAULT '',
  status TEXT DEFAULT 'Новый собственник',
  notes TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  documents TEXT DEFAULT '{}',
  condition TEXT DEFAULT '',
  location_line TEXT DEFAULT '',
  contract_kind TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS owners_pomescheniya (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  district TEXT DEFAULT '',
  address TEXT DEFAULT '',
  area TEXT DEFAULT '',
  price NUMERIC DEFAULT 0,
  contract_type TEXT DEFAULT '',
  status TEXT DEFAULT 'Новый собственник',
  notes TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  documents TEXT DEFAULT '{}',
  condition TEXT DEFAULT '',
  location_line TEXT DEFAULT '',
  contract_kind TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS owners_zemlya (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  district TEXT DEFAULT '',
  address TEXT DEFAULT '',
  area TEXT DEFAULT '',
  area_unit TEXT DEFAULT 'сот',
  price NUMERIC DEFAULT 0,
  contract_type TEXT DEFAULT '',
  status TEXT DEFAULT 'Новый собственник',
  notes TEXT DEFAULT '',
  broker TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  documents TEXT DEFAULT '{}',
  condition TEXT DEFAULT '',
  location_line TEXT DEFAULT '',
  contract_kind TEXT DEFAULT ''
);

CREATE TABLE IF NOT EXISTS profile_links (
  user_id UUID,
  profile_id BIGINT,
  created_at TIMESTAMPTZ DEFAULT now(),
  PRIMARY KEY (user_id, profile_id)
);

CREATE TABLE IF NOT EXISTS profiles (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id UUID,
  username TEXT,
  email TEXT,
  full_name TEXT DEFAULT '',
  role TEXT DEFAULT 'user',
  pin TEXT DEFAULT '',
  phone TEXT DEFAULT '',
  avatar_color TEXT DEFAULT 'blue',
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT now(),
  first_name TEXT DEFAULT '',
  last_name TEXT DEFAULT '',
  password_enc TEXT DEFAULT '',
  notification_settings JSONB
);

CREATE TABLE IF NOT EXISTS properties (
  id UUID DEFAULT gen_random_uuid(),
  title TEXT DEFAULT '',
  price NUMERIC DEFAULT 0,
  rooms INTEGER,
  address TEXT DEFAULT '',
  description TEXT DEFAULT '',
  image_url TEXT DEFAULT '',
  created_at TIMESTAMPTZ DEFAULT now(),
  image_urls JSONB,
  city TEXT DEFAULT '',
  building_type TEXT DEFAULT '',
  complex_name TEXT DEFAULT '',
  year_built INTEGER,
  area NUMERIC,
  bathroom TEXT DEFAULT '',
  ceiling_height NUMERIC,
  status TEXT DEFAULT 'Активно',
  property_type TEXT DEFAULT '',
  contract_number TEXT DEFAULT '',
  payment_method TEXT DEFAULT '',
  contacts TEXT DEFAULT '',
  PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS residential_complexes (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE IF NOT EXISTS task_assignees (
  task_id BIGINT,
  assignee_id BIGINT,
  PRIMARY KEY (task_id, assignee_id)
);

CREATE TABLE IF NOT EXISTS tasks (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title TEXT DEFAULT '',
  client TEXT DEFAULT '',
  due_date TEXT DEFAULT '',
  priority TEXT DEFAULT 'Средний',
  status TEXT DEFAULT 'В работе',
  created_at TIMESTAMPTZ DEFAULT now(),
  description TEXT DEFAULT '',
  created_date TEXT DEFAULT '',
  completed_at TIMESTAMPTZ,
  documents TEXT DEFAULT '{}'
);
